apply plugin: "com.android.application"

android {
    namespace = "com.thuc.pushlog"
    compileSdk = 36

    defaultConfig {
        applicationId = "com.thuc.pushlog"
        minSdk = 26
        targetSdk = 36
        versionCode = 10
        versionName = "1.3.0"
    }

    buildTypes {
        release {
            minifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}
